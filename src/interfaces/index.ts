interface Filters {
    handicapped: boolean,
    changingstation: boolean,
    free: boolean
}

interface Coordinates{
    latitude:number;
    longitude:number;
  }

export type {
    Filters, Coordinates
}


